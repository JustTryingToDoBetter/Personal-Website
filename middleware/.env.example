# Example environment variables for the Node.js middleware

# HTTP Basic authentication credentials used to protect the middleware.
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=changeme

# URL of the Django API including the `/api` prefix.  When running the
# application in Heroku this is bound to the internal port 8000 via
# `start-prod.sh`, so the default below is appropriate.
DJANGO_API_URL=http://localhost:8000/api

# Set to `production` when running in Heroku to serve the built Vue app.
NODE_ENV=development