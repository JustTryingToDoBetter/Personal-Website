{
  "name": "jaydin-morrison-site",
  "private": true,
  "workspaces": [
    "frontend",
    "middleware"
  ],
  "scripts": {
    "heroku-postbuild": "npm install --prefix frontend && rm -rf frontend/.next && npm run build --prefix frontend"
  },
  "dependencies": {}
}
